shape Counter {
    value: u8,

    fun risky(self: ref<Self>) -> Void raises { Bad } {
        raise Bad
    }

    fun wrapper(self: ref<Self>) -> Void raises {
        check self.risky()
    }
}

pub fun main() -> Void {
    let counter: Counter = Counter { value: 0 }
    counter.wrapper()
}
